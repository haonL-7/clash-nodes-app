{
  "name": "clash-nodes-app",
  "version": "1.0.0",
  "description": "Free proxy subscription & node directory",
  "author": "haonl-7",
  "main": "app/main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . \"Clash Nodes\" --platform=win32 --arch=x64 --out=release --overwrite"
  },
  "devDependencies": {
    "@electron/packager": "^18.0.0",
    "electron": "^35.0.0"
  }
}
