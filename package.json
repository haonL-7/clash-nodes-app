{
  "name": "clash-nodes-app",
  "version": "1.0.0",
  "description": "Free proxy subscription & node directory",
  "author": "haonl-7",
  "main": "app/main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . \"Clash Nodes\" --platform=win32 --arch=x64 --out=release --overwrite --asar --prune=true"
  },
  "devDependencies": {
    "electron": "^33.0.0",
    "electron-packager": "^17.1.2"
  }
}
